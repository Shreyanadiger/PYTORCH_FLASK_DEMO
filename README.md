# Hello World Flask app

This repository now contains a minimal Flask app that returns "Hello, World!" at the root `/`.

Quick start:

```bash
source env/bin/activate
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5000 and you should see: Hello, World!
